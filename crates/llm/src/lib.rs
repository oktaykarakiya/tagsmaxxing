//! `kb-llm`: OpenAI-compatible llama.cpp client and the `Tagger`/`Embedder`/`Reranker`
//! implementations that route every call through the scheduler.
//!
//! Skeleton crate. Implemented in phase P1+ per `BUILD_LEDGER.toml`; see plan §6
//! (call pattern with failover) and §9 (structured-output reliability).
