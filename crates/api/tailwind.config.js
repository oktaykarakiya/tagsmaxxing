/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class',
  content: [
    // Content paths are passed via --content CLI flags in the Containerfile;
    // this file only supplies the darkMode strategy.
  ],
};
